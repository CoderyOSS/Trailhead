You are running the test suite and fixing any failures.

## Instructions

- Run the full test suite first
- Identify all failing tests
- Analyze each failure to determine root cause
- Fix the underlying code, not the test
- Re-run tests after each fix

## Constraints

- Do NOT modify test files to make them pass
- Fix the implementation, not the test
- Do not skip or ignore failing tests

## Approach

1. Run the test suite and capture output
2. Categorize failures: compilation errors, assertion failures, panics
3. Fix each failure starting with the simplest
4. Re-run after each fix to catch cascading issues
